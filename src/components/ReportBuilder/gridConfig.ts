// Centralized grid configuration for the Report Builder
// Avoid hard-coded values in components – import these instead

export const GRID_ROW_HEIGHT = 60; // must be in sync with design tokens if exposed later
export const GRID_DEFAULT_MAX_ROWS = 12; // sensible cap to prevent runaway auto-height
export const GRID_MARGIN: [number, number] = [16, 16];
